# Scripts

## `validate_inventory.py`

Checks required columns, duplicate IDs, enumerated values, authority ranks and ISO dates.

## `validate_json_files.py`

Checks that JSON and JSONL files parse successfully.

## `build_distributions.py`

Compiles canonical Markdown into provider-specific distribution folders and creates a release manifest with SHA-256 hashes.
